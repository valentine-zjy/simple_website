# Simple Next.js Vercel Site

这是一个最简单的 Next.js 网站，可以直接部署到 Vercel。

## 本地运行

```bash
npm install
npm run dev
```

打开 http://localhost:3000

## Vercel 部署

1. 上传到 GitHub。
2. 在 Vercel 导入这个仓库。
3. Framework Preset 选择 Next.js。
4. Build Command 使用 `npm run build`。
5. Output Directory 留空。
6. 点击 Deploy。
